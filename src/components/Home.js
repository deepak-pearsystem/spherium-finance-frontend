import React from 'react';
import Banner from './Banner';

const Home = () => {
    return (
        <section className="home-wrapper">
            <Banner/> 
        </section>
    );
};

export default Home;